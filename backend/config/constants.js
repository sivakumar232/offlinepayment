// config/constants.js

// Define global constants for the system
const OFFLINE_LIMIT = 500; // Stage 0 requirement: ₹500

module.exports = {
    OFFLINE_LIMIT,
    // (Other system constants can be added here later, e.g., CURRENCY, HASHING_ALGORITHM)
};